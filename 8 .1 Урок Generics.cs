using System;
using System.Collections.Generic;
using System.Collections;
using static System.Console;

/*-Змейка
Входные данные N, M (100 > N, M > 0)
Исходя из полученных входных данных строится маршрут шагов змейкой. 
Например: 
2,3     4,4
1 6     1 8 9  16
2 5     2 7 10 15
3 4     3 6 11 14
        4 5 12 13*/


class Program
{
    static void Main()
    {
        int N, M;

        WriteLine("Enter Two Number: ");
        N = Int32.Parse(ReadLine());
        M = Int32.Parse(ReadLine());
        int[,] mas = new int[N,M];

        for (int i = 0; i < N; i++)
        {
            for(int j = 0; j < M; j++)
            {
                if (t <= M)
                {
                    
                }
            }
        }
    }
}

